#includepath "~/Documents/;%USERPROFILE%Documents";#include "basiljs/bundle/basil.js";var cols;var rows;var cells = 40;function setup() {    	b.clear(b.page()); 	b.units(b.MM);	b.rectMode(b.CENTER);	b.colorMode(b.CMYK);    	b.canvasMode(b.MARGIN);		cols = b.floor(b.width / cells);	rows = b.floor(b.height / cells);    	var cw = b.width / cols;	var ch = b.height / rows;	for(var i = 0; i < cols; i++){		for(var j = 0; j < rows; j++){						b.pushMatrix();			var posX = i * cw + cw/2;			var posY = j * ch + ch/2;						b.noFill();			b.stroke(255,255,255,255);						b.strokeWeight(3);								    var color = b.floor(b.random(1, 9));								if ( color  == 1){				b.fill(100, 0, 0, 0);			} else if ( color  == 2){				b.fill(0, 100, 0, 0);			} else if ( color  == 3){				b.fill(0, 0, 100, 0);			} 						b.rect(posX, posY, cw, ch);						var num = b.floor(b.random(1, 5));								if ( num == 1){				b.line(posX, posY-ch/2, posX ,posY-ch/2);			} else if ( num == 2){				b.line(posX-cw/2, posY, posX+cw/2, posY);			} else if ( num == 3){				//b.line(-cw/2, 0, cw/2, 0);			} else if ( num == 4){				//b.line(0, -ch/2, 0 , ch/2);			}						b.popMatrix();		}		}	}b.go();